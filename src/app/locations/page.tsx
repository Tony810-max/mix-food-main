import LocationsPage from '@/modules/LocationPage';
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Locations',
  description: 'Locations Restaurant Thailand Food',
};

export default LocationsPage;
