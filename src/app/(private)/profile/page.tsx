import ProfilePage from '@/modules/ProfilePage';
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Profile',
  description: 'Profile Restaurant Thailand Food',
};

export default ProfilePage;
