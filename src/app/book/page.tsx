import BookingPage from '@/modules/BookingPage';
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Book',
  description: 'Book Restaurant Thailand Food',
};

export default BookingPage;
