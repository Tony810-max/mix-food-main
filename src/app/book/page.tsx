import BookingPage from '@/modules/BookingPage';
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Book',
  description: 'Đặt bàn ngay tại Mix Food',
};

export default BookingPage;
